import { Routes } from '@angular/router';
import { ResponseComponent } from '../response/response.component';

export const RESPONSE_ROUTES: Routes = [
    { path: '', component: ResponseComponent }
];